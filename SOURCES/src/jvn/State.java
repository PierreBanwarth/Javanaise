package jvn;

public enum State {
	
	NL, RC, WC, R, W, RWC;
	// no lock
	// Read lock cached 
	// write lock cached 
	// Read lock taken
	// Write lock taken
	// read lock taken write lock cached
	
	
}